import { defineComponent } from "bitecs";

export const InputSource = defineComponent();
export const FirstInputSource = defineComponent();
export const SecondInputSource = defineComponent();

export const InputSourceTriggered = defineComponent();
export const InputSourceReleased = defineComponent();
